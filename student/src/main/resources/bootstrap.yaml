spring:
  profiles:
    active: dev
  application:
    name: nacos-student

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        file-extension: yaml
        # 配置文件的地址也需要指定
        server-addr: localhost:8848
        group: DEV_GROUP
        namespace: ea6b6195-9373-4594-8ee8-c3ed17bbb226

